/// 活动期子域:围绕当前进行中的活动(`Campaign`)提供的用例,目前只有查询,没有写路径。
///
/// - `query/`:三个只读查询用例——充值榜 Top10、奖池实况(按档位)、近期充值动态 Top20,
///   全部走同一降级口径:无进行中活动 → 空列表,不是异常
///
/// 没有 `command/`、`dto/` 子包不是遗漏:活动期本身(`Campaign` 的创建/开始/结束)不由本平台的应用代码写入,
/// 由运维 SQL 维护,这个子域至今没有长出写用例。
package me.supernb.activity.app.usecase.campaign;
