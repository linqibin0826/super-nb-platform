/// 业务规则与不变量的纯计算存放处(状态判断、资格校验一类,不是持久化实体也不是读视图)。
///
/// - `read/`:读侧视图 record(见该包 package-info)
/// - `enums/`:领域枚举
///
/// gallery 上下文目前没有这类计算类型直接落在这一层——业务规则简单到不需要独立建模,
/// 判断逻辑就近散在各自用例里;这层当前是纯粹的子包容器,没有直接类型。
package me.supernb.gallery.domain.model;
